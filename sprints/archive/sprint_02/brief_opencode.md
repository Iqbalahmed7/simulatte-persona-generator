# SPRINT 2 BRIEF — OPENCODE
**Role:** Idle (Domain Template Expansion Prep)
**Sprint:** 2 — Identity Constructor
**Previous rating:** 16/20 — Good domain templates. Missing __init__.py patched. Zero base taxonomy overlap.

---

## Your Job This Sprint

OpenCode is idle this sprint. The Identity Constructor (Sprint 2) is domain-agnostic — domain templates are not involved in the base identity build sequence.

Use this sprint to prepare for Sprint 3 (Memory Architecture), where you will own `core_memory.py` and `seed_memory.py`.

---

## Preparation Task (Not a Deliverable — No Outcome File Required)

Read the following in preparation for Sprint 3:

1. **Master Spec §8** — Memory Architecture (all subsections). Focus on:
   - Core vs working memory split
   - Seed memory bootstrap rules (≥ 3 seed memories)
   - Promotion rules (importance ≥ 9, ≥ 3 citations, no contradiction)
   - What goes into CoreMemory vs what is generated by IdentityConstructor

2. **`src/schema/persona.py`** — Read `CoreMemory`, `WorkingMemory`, `Observation`, `Reflection` to understand the data structures you'll be populating.

3. **`src/generation/identity_constructor.py`** (once Cursor writes it) — Understand how `_assemble_core_memory()` works, because your `core_memory.py` will be a more complete version of that assembly logic.

You will be asked to write `src/memory/core_memory.py` and `src/memory/seed_memory.py` in Sprint 3. Being prepared means you will need zero ramp-up time when that brief arrives.

---

## No Outcome File Required This Sprint.
