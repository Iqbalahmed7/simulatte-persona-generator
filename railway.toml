[build]
dockerfilePath = "Dockerfile.the-mind"

[deploy]
startCommand = "uvicorn pilots.the_mind_api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
